@using EntityLayer.Concrete

@model List<Job>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_UILayout.cshtml";
}


<div class="container-xxl flex-grow-1 container-p-y">
    <h4 class="fw-bold py-3 mb-4">
        <span class="text-muted fw-light">Meslek  Yönetimi /</span> Meslekler
    </h4>

    <!-- Basic Bootstrap Table -->
    <div class="card">
        <a class="btn btn-success"  asp-controller="Job" asp-action="Add">Yeni Müşteri ekle <i class="bx bx-plus"></i></a>
        <div class="table-responsive text-nowrap m-5">



            <table class="table table-bordered" id="articlesTable" width="100%" cellspacing="0">
                <thead>
                    <tr>
                        <th>Meslek</th>
                     
                   
                       

                    </tr>
                </thead>
                <tfoot>
                    <tr>
                        <th>Müşteri ismi</th>
                      
                    
                      

                    </tr>
                </tfoot>
                <tbody class="table-border-bottom-0">
                    @foreach (var job in Model)
                    {
                        <tr name="@job.JobId">
                            <th>@job.Name</th>
                         
                         
                           
                            
                            <th>

                                <a class="btn btn-primary btn-sm" data-id="@job.JobId" asp-controller="Job" asp-action="Update" asp-route-Id="@job.JobId"><i class="bx bxs-edit"></i></a>
                                <a class="btn btn-danger btn-sm" data-id="@job.JobId" asp-controller="Job" asp-action="Delete" asp-route-Id="@job.JobId"><i class="bx bx-minus-circle"></i></a>


                            </th>

                        </tr>


                    }
                </tbody>
            </table>
        </div>
    </div>
    <!--/ Basic Bootstrap Table -->




</div>

@section Scripts {
    <script src="~/js/articleIndex.js" type="application/ecmascript"></script>
}

